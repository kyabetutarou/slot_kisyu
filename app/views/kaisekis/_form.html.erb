<%= form_for(@kaiseki) do |f| %>
  <% if @kaiseki.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@kaiseki.errors.count, "error") %> prohibited this kaiseki from being saved:</h2>

      <ul>
      <% @kaiseki.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :kiji %><br>
    <%= f.text_area :kiji %>
  </div>
  <div class="field">
    <%= f.label :slot_name_id %><br>
    <%= f.text_field :slot_name_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
